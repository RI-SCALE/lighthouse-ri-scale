# ---------------------------------------------------------------------------
# Lighthouse Trust Anchor – environment variables
# Copy this file to .env if Docker Compose requires it.
#
# NOTE: entity_id is set directly in lighthouse/config.yaml.
# When deploying via Ansible, the config.yaml is rendered from a Jinja2
# template with the correct value baked in.
# ---------------------------------------------------------------------------
